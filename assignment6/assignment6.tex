\documentclass[a4paper,11pt]{article}

% ---------- Packages ----------
\usepackage[margin=2.5cm]{geometry}
\usepackage{amsmath, amssymb, amsthm, mathtools}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xcolor}

\usepackage{tikz}
\usetikzlibrary{automata,positioning,arrows.meta}

% ---------- Theorem Environments ----------
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{definition}{Definition}
\newtheorem{proposition}{Proposition}
\newtheorem{corollary}{Corollary}

% ---------- Custom Commands ----------
\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\Alphabet}{\Sigma}
\newcommand{\eps}{\varepsilon}
\newcommand{\set}[1]{\left\{ #1 \right\}}
\newcommand{\abs}[1]{\left| #1 \right|}
\newcommand{\ceil}[1]{\left\lceil #1 \right\rceil}
\newcommand{\floor}[1]{\left\lfloor #1 \right\rfloor}

% ---------- Section Style ----------
\renewcommand{\thesubsection}{(\alph{subsection})}
\renewcommand{\thesection}{}

% ---------- Algorithm Style ----------
\algrenewcommand\algorithmicrequire{\textbf{Input:}}
\algrenewcommand\algorithmicensure{\textbf{Output:}}

% ---------- Title Info ----------
\title{\textbf{CSE2315 --- Assignment 6}}
\author{Vlad Paun \\ 6152937}
\date{\today}

% ---------- Document ----------
\begin{document}

	\maketitle

	\section{Exercise 1}
	Suppose we have the following language:
	\[
	E_{\mathrm{GNFA}} = \set{\langle G \rangle \mid G \text{ is a GNFA such that } L(G)=\emptyset }.
	\]

	Show that $E_{\mathrm{GNFA}}$ is decidable. You are allowed to make use of the deciders implied by the text in the book (e.g., Section 4.1).

	\textbf{Answer: }We show that $E_{GNFA}$ is decidable by giving decider $M$ as follows:
	\begin{quote}
		$M=$ "On input $\langle G\rangle$:
		\begin{enumerate}
			\item Convert GNFA $G$ to an equivalent DFA $D$
			\item Run the decider for $E_{DFA}$ on $\langle D\rangle$
			\item If the decider accepts, accept. Otherwise, reject.
		\end{enumerate}
	\end{quote}

	\section{Exercise 2}
	Consider the language
	\[
	L = \set{\langle M,w \rangle \mid M \text{ halts on } w \text{ and moves the head at most } |w|^2 \text{ steps}}.
	\]

	Is this language decidable? Explain.

	\textbf{Answer: }$L$ is decidable. A TM must move the head every step. So we can simulate $M$ for at most  steps. If it halts before reaching this limit we can accept. Otherwise, $M$ is either looping or it moves the head more that $|w|^2$ steps, so we can reject.

	\section{Exercise 3}
	Old exam question, endterm 20--21: Show the claim is true or give a counterexample and explanation to show the claim is false, starting your answer with either \textbf{True} or \textbf{False}.

	If there is a Turing reduction from $L$ to $E_{\mathrm{CFG}}$, then $\overline{L}$ is Turing-recognisable.

	\textbf{True.} If there is a reduction from $L$ to $E_{CFG}$, that means that $L$ is no harder than $E_{CFG}$. So if the latter is decidable, which it is, then so is the former. So $L$ is also decidable. Decidability is closed under complement, therefore $\overline{L}$ is also decidable. Any decidable language is also Turing-recognizable.

	\section{Exercise 4}
	Old exam question, endterm 22--23:

	Consider the following language:
	\[
	L = \set{\langle M,q \rangle \mid M \text{ is a Turing Machine and } q \text{ is a useless state in } M }.
	\]

	A useless state in a Turing Machine is one that is never entered on any input string.

	Prove $L$ is undecidable, using a Turing reduction $E_{\mathrm{TM}} \leq_T L$.

	\begin{proof}
		Proof by contradiction.\\
		Assume for the sake of contradiction that $L$ is decidable. Then, there exists TM $S$ that decides $L$. We can use $M$ to show that $E_{TM}$ is also decidable by constructing decider $R$:
		\begin{quote}
			$R=$ "On input $\langle M\rangle$:
			\begin{enumerate}
				\item Scan through the input and find the accept state of $M$, $q_{accept}$
				\item Run $S$ on $\langle M,q_{accept}\rangle$
				\item If $S$ accepts, accept, otherwise reject
			\end{enumerate}
		\end{quote}
		Thus, if this machine accepts, the accept state of the input is useless so its language is empty and vice versa. So $R$ correctly decides $E_{TM}$, which is a contradiction with theorem $5.2$
	\end{proof}

	\section{Exercise 5}
	Old exam question, resit 17--18: Suppose we have the following language
	\begin{align*}
	L = \{\langle M,v,G \rangle \mid & M \text{ outputs } G \text{ on the tape, which is a context free grammar}\\
	&\text{that accepts } v,\text{ when run on input } v.\}
	\end{align*}

	We intend to prove that this language is undecidable. To that end we use a mapping reduction from $HALT_{TM}$ to $L$. The reduction $f$ is computed by the following TM $F$:

	\begin{quote}
		$F =$ ``On input $\langle M,w \rangle$:
		\begin{enumerate}[label=\arabic*.]
			\item Construct the following TM $M'$:
			\begin{quote}
				$M' =$ ``On input $x$:
				\begin{enumerate}[label=\arabic*.]
					\item Clear the input from the tape.
					\item Run $M$ on $w$.
					\item See question a.
					\item See question a.
				\end{enumerate}
				''
			\end{quote}
			\item Write $\langle M',a,G \rangle$ to the output tape, where $G$ is a grammar with one rule: $S \to a$ and starting variable $S$.''
		\end{enumerate}
	\end{quote}

	\subsection{What should be done in steps 3 and 4 of $M'$?}

	3. Generate a grammar $G$ with one rule: $S\to a$ and starting variable $S$.\\
	4. Output grammar $G$ on the tape.

	\subsection{Provide a proof showing $f$ satisfies the requirements of a mapping reduction.}

	\begin{proof}
		$F$ computes $f$, so $f$ is a computable function. We also need to show that it is reliable. To this end, we look at two cases:
		\begin{itemize}
			\item If $\langle M,w\rangle\in HALT_{TM}$, then $M$ halts on $w$ and $M'$ also halts and correctly writes the grammar $G$ on input $a$.
			\item If $\langle M,w\rangle\notin HALT_{TM}$, then $M$ never halts on $w$, so $M'$ will never write the correct output on the tape.
		\end{itemize}
	\end{proof}

	\section{Exercise 6}
	Old exam question, resit 20--21:

	\subsection{Let $MIA_{TM}$ be the language consisting of 3 TM's and a word such that the word is recognized by at least 2 of the 3 TM's. Show that this is a recognizable language.}
	Formally described as
	\begin{align*}
	MIA_{TM} = \{\langle M_1,M_2,M_3,w \rangle &\mid M_1,M_2,M_3 \text{ are TM's and }\\
	& w \in (L(M_1)\cap L(M_2)) \cup (L(M_1)\cap L(M_3)) \cup (L(M_2)\cap L(M_3)) \}.
	\end{align*}

	\textbf{Answer: }We show this by constructing a TM $R$ that recognizes $MIA_{TM}$:
	\begin{quote}
		$R=$ "On input $\langle M_1,M_2,M_3,w \rangle$:
		\begin{enumerate}
			\item Simulate $M_1,M_2,M_3$ on $w$ in parallel
			\item As soon as 2 of these machines accept, accept.
			\item If 2 or more reject, reject."
		\end{enumerate}
	\end{quote}

	\subsection{Show that $MIA_{TM}$ is undecidable by constructing a Turing reduction from $A_{TM}$.}
	\begin{proof}
		Assume for the sake of contradiction that $MIA_{TM}$ is decidable. Therefore, there exists TM $S$ that decides $MIA_{TM}$. We then construct TM $N$ as follows:
		\begin{quote}
			$N=$ "On input $\langle M,w \rangle$:
			\begin{enumerate}
				\item Create TM $M_{none}$ that instantly rejects any input
				\item Create TM $M_{all}$ that instantly accepts any input
				\item Run $S$ on $\langle M,M_{none},M_{all},w \rangle$
				\item If it accepts, accept, otherwise reject."
			\end{enumerate}
		\end{quote}
		We can see that from the description of $MIA_{TM}$, $S$ accepts $w$ iff $M$ accepts $w$ and we have assumed that it is a decider so it always halts. Thus, $N$ is a decider for $A_{TM}$, which is a contradiction. Thus, our assumption that $MIA_{TM}$ is decidable must be false, so, by contradiction we have shown that $MIA_{TM}$ is undecidable.
	\end{proof}

	\subsection{Show that $MIA_{TM}$ is not co-Turing recognizable.}

	We have shown that $MIA_{TM}$ is Turing recognizable. If $\overline{MIA_{TM}}$ were also Turing recognizable, then $MIA_{TM}$ would be decidable, which we have shown it is not. Therefore, $\overline{MIA_{TM}}$ must be Turing unrecognizable, so $MIA_{TM}$ is not co-Turing recognizable.

	\section{Exercise 7}
	Revision, old exam question, midterm 19--20: Assume that we define an expanded NFA model by imposing the additional requirement that $|F|=1$, and we call this an SNFA.

	\subsection{ Are the SNFA and NFA models equally expressive?}
	Start your answer with ``Yes'' or ``No''. If you answer yes, explain how an arbitrary SNFA can be rewritten as an NFA and vice versa. If you answer no, give an example of a language that can be accepted by an NFA, but not by an SNFA or vice versa.

	\textbf{Yes.} An $SNFA$ is a special case of an $NFA$, so an arbitrary $SNFA$ is already an $NFA$. To transform an arbitrary $NFA$ to an $SNFA$, we add a new accepting state, we take all the old accepting states and make them not-accepting and add an epsilon-transition to the new single accepting state.

	\subsection{Give an NFA of at most 4 states that accepts the word \texttt{mia} and rejects the word \texttt{maya}. No other restrictions on the language apply. A transition diagram suffices.}

	\begin{center}
		\begin{tikzpicture}[->, >=Stealth, auto, node distance=3cm]
			\node[initial,state] (q0) {$q_0$};
			\node[state] (q1) [right of=q0] {$q_1$};
			\node[state] (q2) [right of=q1] {$q_2$};
			\node[state,accepting] (q3) [right of=q2] {$q_3$};

			\path
			(q0) edge node {$m$} (q1)
			(q1) edgenode {$i$} (q2)
			(q2) edge node {$a$} (q3);

		\end{tikzpicture}
	\end{center}



	\subsection{Consider the NFA $N$ depicted as:}

	\begin{center}
		\begin{tikzpicture}[->, >=Stealth, auto, node distance=3cm]
			\node[initial,state] (q0) {$q_0$};
			\node[state, accepting] (q1) [right of=q0] {$q_1$};
			\node[state,accepting] (q2) [right of=q1] {$q_2$};

			\path
			(q0) edge[loop above] node {$b$} (q0)
			(q0) edge[bend left] node {$a,b$} (q1)
			(q1) edge[bend left] node {$b$} (q0)
			(q1) edge[bend left] node {$b$} (q2)
			(q2) edge[loop above] node {$a$} (q2)
			(q2) edge[bend left] node[below] {$a,b,\eps$} (q1)
			(q2) edge[bend left=60] node[below] {$\eps$} (q0);
		\end{tikzpicture}
	\end{center}

	Using the method from Sipser and leaving out unreachable states, construct a new DFA $D$, such that $L(D)=L(N)$. A transition diagram and a short explanation suffice.

	\begin{center}
		\begin{tikzpicture}[->, >=Stealth, auto, node distance=3cm]
			\node[initial,state] (q0) {$q_0$};
			\node[state, accepting] (q1) [right of=q0] {$q_1$};

			\node[state, accepting] (q01) [below of=q0] {$q_{01}$};


			\node[state, accepting] (q012) [below of=q01] {$q_{012}$};
			\node[state] (qnone) [below of=q1] {$q_{none}$};

			\path (q0) edge node {$a$} (q1);
			\path (q0) edge node {$b$} (q01);
			\path (q1) edge node {$a$} (qnone);
			\path (q1) edge node {$b$} (q012);
			\path (q01) edge node {$a$} (q1);
			\path (q01) edge node {$b$} (q012);
			\path (qnone) edge [loop below] node {$a,b$} (qnone);
			\path (q012) edge [loop right] node {$a,b$} (q012);
		\end{tikzpicture}
	\end{center}

\end{document}