\documentclass[a4paper,11pt]{article}

% ---------- Packages ----------
\usepackage[margin=2.5cm]{geometry}
\usepackage{amsmath, amssymb, amsthm, mathtools}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xcolor}

\usepackage{tikz}
\usetikzlibrary{automata,positioning,arrows.meta}

% ---------- Theorem Environments ----------
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{definition}{Definition}
\newtheorem{proposition}{Proposition}
\newtheorem{corollary}{Corollary}

% ---------- Custom Commands ----------
\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\Alphabet}{\Sigma}
\newcommand{\eps}{\varepsilon}
\newcommand{\set}[1]{\left\{ #1 \right\}}
\newcommand{\abs}[1]{\left| #1 \right|}
\newcommand{\ceil}[1]{\left\lceil #1 \right\rceil}
\newcommand{\floor}[1]{\left\lfloor #1 \right\rfloor}

% ---------- Section Style ----------
\renewcommand{\thesubsection}{(\alph{subsection})}
\renewcommand{\thesection}{}

% ---------- Algorithm Style ----------
\algrenewcommand\algorithmicrequire{\textbf{Input:}}
\algrenewcommand\algorithmicensure{\textbf{Output:}}

% ---------- Title Info ----------
\title{\textbf{CSE2315 --- Assignment 7}}
\author{Vlad Paun \\ 6152937}
\date{\today}

% ---------- Document ----------
\begin{document}

	\maketitle

	\section{Exercise 1}
	Prove that the following problem is in NP:
	\[
	\set{(V,E) \mid \text{the graph } G=(V,E) \text{ contains a simple path } \pi \text{ such that } \forall v \in V : v \in \pi}
	\]

	In a different notation we would write:\\
	\textbf{Name:} Hamiltonian Cycle (HAMC)\\
	\textbf{Input:} A graph $G=(V,E)$.\\
	\textbf{Question:} Is there a simple cycle $\pi$ in $G$ such that all vertices $v \in V$ are in that cycle?

	\begin{proof}
		The following is a verifier $V$ for $HAMC$:
		\begin{quote}
			$V=$ "On input $\langle G,c \rangle$:
			\begin{enumerate}
				\item Test whether $c$ is a tuple containing all nodes in $G$ exactly once.
				\item For each $i$ between 1 and $m-1$ (where $m$ is the number of nodes in $G$): check that there is an edge in $G$ from node $c_i$ to $c_{i+1}$
				\item Check that there is an edge in $G$ from $c_m$ to $c_1$
			\end{enumerate}
		\end{quote}

		Stages $1.$ and $3.$ run exactly once, and stage $2.$ runs m times, so there is a polynomial amount of stages. We can easily see that each stage can be completed in polynomial time, so the verifier is overall polynomial. Therefore, $HAMC\in NP$
	\end{proof}



	\section{Exercise 2}
	Old exam question, endterm 23--24:

	For the following claim, if it is true: prove it. If it is false, give an explanation showing the claim is false and if possible a counterexample. Remember that the cover of our exam reads: ``Unless otherwise stated we assume that $P \neq NP$ and that $NP \neq EXP$.''

	For three languages $A, B, C$ if $A \leq_P B$ and $B \leq_m C$ with $C \in NPC$. Furthermore we know that there is a regular expression $R$ such that $L(R)=A$. Then $B \in P$.

	\textbf{False. }Take for example $A=\emptyset$, which is regular, and $B=C=SAT$. $A$ is karp reducible to B by a function that takes any input ignores it and outputs $p\wedge\overline{p}$. $B$ is equal to $C$ so it is mapping reducible to it by a function that does nothing. And finally $C\in NPC$. So all 3 conditions hold. However, $B=SAT\in NP$ and we assume $P\neq NP$.

	\section{Exercise 3}
	Old exam question:

	On the (famous?) British Quiz Show ``The University Challenge'', the following question was asked in the grand final of 2018/2019.

	\begin{quote}
		\ldots which problem in computing asks whether all problems with a solution verifiable in finite time, can also be solved in finite time.
	\end{quote}

	The contestants answer with ``P equals NP'' and the host answers back with ``Yes that's right, P versus NP, yes''.

	Assuming this is the answer the host was looking for, what is wrong with the question he asks?

	\textbf{Answer: }The question asks about finite time, which is trivial. The correct question should ask about solutions verifiable and solvable in polynomial time.

	\section{Exercise 4}
	Old exam question, resit 20--21:

	\subsection{Prove that the following problem is in NP.}

	\textbf{Name:} AmazingCustomChallenge\\
	\textbf{Instance:} Given a set $L$ of lectures, a set of students $S$, a collection of subsets $T$ with $|T|=|S|$ so that $T_i$ is the set of lectures that student $i$ needs to attend, and an integer $K$.\\
	\textbf{Question:} Is there a function $f:L \to \set{1,2,\dots,K}$ that maps lectures to time slots so that all students can attend all their lectures. In other words, is there a function so that there is no student who has to attend two lectures that are mapped to the same time slot.

	\begin{proof}
		The following is a NTM $N$ that decides AmazingCustomChallenge in nondeterministic polynomial time:
		\begin{quote}
			$N=$ "On input $\langle L,S,T,K\rangle$:
			\begin{enumerate}
				\item For each lecture $l\in L$, nondeterministically choose a value $f(l)\in\set{1,\dots,K}$
				\item For every student $i$ check that no lectures in $T_i=\set{l_{i_1},\dots,l_{i_x}}$ are assigned the same time-slot by looking them up for every pair $(l_{i_m},l_{i_n})$
				\item Accept if no conflict is found, reject otherwise
			\end{enumerate}
		\end{quote}
		Stage one has to guess $|L|$ numbers, and writing down each number would take $O(\log K)$, so overall stage 1 takes $O(|L|\log K)$. Stage 2 needs to check all pairs of lectures in every $T_i$, so this would be bounded by $O(\sum_{i=1}^{|S|}|T_i|^2)$, which would be a worst case of $O(|S|\cdot |L|^2)$. Stage 3 is constant. Therefore, overall machine N runs in $O(|L|\log K + |S|\cdot|L|^2)$, which is polynomial in the size of the input.
	\end{proof}

	\subsection{Not originally part of the exam question. Your answer to the previous question should have included time complexities. If we are to describe them in terms of $|I|$ where $I$ is an instance of the problem, what would be the complexity? (Hint: first think about $|I|$, is it $|S|$, or $|L|$, or some combination? How does $K$ factor into this?)}

	\begin{align*}
		|I| &\in O(|L|+|S|+\sum_{i}|T_i|+\log K)\\
		|I| &\in O(|L|+|S| + |S|\cdot|L| + \log K)\\
	\end{align*}

	So, overall the runtime could be expressed roughly as $T(I)\in O(|I|^3)$.

	\section{Exercise 5}
	Variation on old exam question, resit 20--21:

	Show the claim is true or false, starting your answer with either ``True'' or ``False''.

	Given two problems $A$ and $B$ that can be solved using a PDA, it is possible to create a Karp-reduction from $A$ to $B$ and vice versa.

	\textbf{False. } Take for example $A=\Alphabet^*$ and $B=\emptyset$. Suppose there would exist a Karp reduction from $A$ to $B$, then there exists a computable function $f: \Alphabet^*\to \Alphabet^*$ such that $x\in A \iff f(x)\in B$. However, in our example $\forall x\in \Alphabet^*: x\in A$. So $\forall x\in \Alphabet^*: f(x)\in B$, but B must be empty. Therefore, no such function can exist.

	\section{Exercise 6}
	Revision, old exam question, resit 20--21: Given this NFA $N$, give a regular expression $R$ so that $L(N)=L(R)$.

	\begin{center}
		\begin{tikzpicture}[->, >=Stealth, auto, node distance=2.4cm]
			\node[initial,state] (q0) {$q_0$};
			\node[state] (q1) [right of=q0] {$q_1$};
			\node[state] (q2) [below of=q0] {$q_2$};
			\node[state, accepting] (q3) [below of=q1] {$q_3$};
			\node[state] (q4) [right of=q1] {$q_4$};
			\node[state] (q5) [below of=q4] {$q_5$};

			\path
			(q0) edge[loop above] node {$a,b,c$} (q0)
			(q0) edge node {$b$} (q1)

			(q4) edge node {$c$} (q5)
			(q1) edge node {$a$} (q2)
			(q2) edge node {$c$} (q3)
			(q3) edge[loop right] node {$a,b$} (q3)
			(q3) edge node {$c$} (q4)
			(q5) edge[loop right] node {$a,b,c$} (q5);
		\end{tikzpicture}
	\end{center}

	\begin{align*}
		(a\cup b\cup c)^*bac(a\cup b)^*
	\end{align*}

	\section{Exercise 7}
	Revision, old exam question, endterm 16--17: Suppose we have the following language:
	\[
	L = \set{\langle M,v,w \rangle \mid M \text{ outputs } w \text{ on the tape when run on input } v}
	\]

	Use a mapping reduction to prove that $L$ is undecidable. Give:

	\subsection{A suitable problem to reduce from.}

	We will reduce from $HALT_{TM}$

	\subsection{A suitable reduction function $f:\Sigma^* \to \Sigma^*$.}

	Take $f: \langle M,v\rangle \to \langle M',v,vladulon\rangle$, where:
	\begin{quote}
		$M'=$ "on input $x$:
		\begin{enumerate}
			\item Ignore input $x$.
			\item Simulate $M$ on $v$ and ignore the output.
			\item Output $vladulon$ on the tape"
		\end{enumerate}
	\end{quote}


	\subsection{A proof showing $f$ satisfies the requirements of a mapping reduction.}

	\begin{proof}
		We must show that:
		\begin{enumerate}
			\item $f$ is computable
			\item $\forall\langle M,v\rangle: \langle M,v\rangle\in HALT_{TM} \iff f(\langle M,v\rangle)\in L$
		\end{enumerate}

		For computability, we can trivially see that we can construct the description of $M'$ to just simulate $M$ on the hardcoded input $v$ and then write our specific $vladulon$ string to the tape, and then after that description, append our encoding for $v$ and $vladulon$.\\

		For Correctness, take any input $\langle M,v \rangle$ and consider the 2 cases:
		\begin{enumerate}
			\item $\langle M,v \rangle\in HALT_{TM}$: then $M$ halts on $v$; thus, by construction $M'$ will simulate $M$ on $v$ and eventually halt and after it does so it will write $vladulon$ on the tape. the output of our function $f$ says that $M'$ will correctly output $vladulon$ on the tape when ran on input v, which it does, so $\langle M',v,vladulon\rangle\in L$.
			\item $\langle M,v \rangle\notin HALT_{TM}$; then, $M$ does not halt when ran on $v$. So neither will our machine $M'$ when it simulates $M$ on $v$. So $M'$ will get stuck in a loop before it can print $vladulon$ on the tape. Therefore $M'$ does not output $vladulon$ when ran on $v$, so $\langle M',v,vladulon\rangle\notin L$.
		\end{enumerate}
	\end{proof}


\end{document}